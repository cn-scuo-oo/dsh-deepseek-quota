# ============================================================
#  dsh-deepseek-quota — 环境变量示例（均为可选）
#  一般情况下无需设置：插件从 DSH 应用内 设置→模型 的凭据
#  读取 DEEPSEEK_API_KEY（~/.dsh/.credentials.yaml）。
# ============================================================

# DeepSeek 官方 API 地址（默认官方公网；联调可指向本地 Mock / 公司代理）
# DEEPSEEK_BASE_URL=https://api.deepseek.com
